% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/d_pair.R
\name{d_pair}
\alias{d_pair}
\title{Dominance effect sizes for paired continuous data}
\usage{
d_pair(x, meta = NULL)
}
\arguments{
\item{x}{data. See details.}

\item{meta}{either \code{NULL} or a named list.}
}
\value{
Estimates with verbose explanations.
}
\description{
Calculate dominance-based effect sizes for paired continuous (normally
distributed) data. A convenience wrapper around \code{\link[=d_one]{d_one()}}.
}
\details{
The within-pair differences (e.g., post minus pre) are compared against a
constant of zero. For full control, such as testing against a non-zero
constant, construct the two-row \code{data.frame} (or two-element \code{list}) and
call \code{\link[=d_one]{d_one()}} directly.

Calculate the following:
\itemize{
\item dominance (aka common language effect size);
\item stochastic difference (aka Glass rank-biserial correlation);
\item odds, which, for continuous variables, is a special case of generalized odds ratio; and
\item standardized mean difference (aka Cohen's d).
}

For continuous variables, the following effect sizes simplify to one of the
above-mentioned effect sizes. Thus, they are not reported separately.
\itemize{
\item measure of stochastic superiority (aka Vargha and Delaney's A), which simplifies to dominance;
\item adjusted dominance, which simplifies to dominance; and
\item adjusted stochastic difference (aka Goodman and Kruskal's gamma), which simplifies to stochastic difference; and
\item win odds, which simplifies to odds.
}

\code{x} can contain either summary statistics or microdata.
\itemize{
\item If \code{x} contains summary statistics, it should be a \code{data.frame} with 1 row.
It can optionally contain a variable called \code{group}. In addition, it must
contain one of the following:
\itemize{
\item sample mean (\code{xbar}), sample standard deviation (\code{sd}), and sample size (\code{n}); or
\item \code{xbar}, standard error of the mean (\code{sem}), and \code{n}; or
\item expected value (\code{mu}) and standard deviation (\code{sigma}), which are known parameters.
}
\item If \code{x} contains microdata, it should be a named \code{list} with 1 element
being a vector of \code{numeric} paired differences.
}

Optionally, \code{x} can have an attribute called \code{"meta"}, which must be a named list.
}
\examples{
# Summary statistics. Equivalent to:
# df1 = rbind(comparison_data$IOP, data.frame(group = "Zero", xbar = 0, sd = NA, n = NA))
# d_one(df1)
d_pair(comparison_data$IOP)

# Microdata. Equivalent to:
# d_one(list(differences = c(3.1, -0.5, 2.2, 1.8, 4.0, 0.3), Zero = 0))
d_pair(list(differences = c(3.1, -0.5, 2.2, 1.8, 4.0, 0.3)))
}
\seealso{
Other es: 
\code{\link{d_bin}()},
\code{\link{d_cont}()},
\code{\link{d_np}()},
\code{\link{d_one}()},
\code{\link{d_ord}()}
}
\concept{es}
